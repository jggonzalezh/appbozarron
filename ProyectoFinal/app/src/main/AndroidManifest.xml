<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="co.edu.uniquindio.android.electiva.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.EquiposActivity" />
        <activity android:name=".activity.ParticipantesActivity" />
        <activity android:name=".activity.DetalleParticipanteActivity" />
        <activity android:name=".activity.EntrenadoresActivity" />
        <activity android:name=".activity.ListaparticipanteEntrenadorActivity"></activity>
    </application>

</manifest>